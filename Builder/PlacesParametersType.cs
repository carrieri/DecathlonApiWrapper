namespace DecathlonApiWrapper.Builder
{
    internal enum PlacesParametersType
    {
        origin,
        radius,
        sw,
        ne,
        user_origin,
        sports,
        tags,
        surface,
        min_difficulty,
        max_difficulty,
        min_duration,
        max_duration,
        min_distance,
        max_distance
    }
}
